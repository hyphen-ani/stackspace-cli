import chalk from "chalk";

export function ListStacks() {
    console.log("Returning Stacks")
}

// ListStacks().catch((err) => {
//     console.error(chalk.red(`An error occured:`, err));
//     process.exit(1);
// });